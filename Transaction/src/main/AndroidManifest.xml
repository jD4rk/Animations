<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.jdark.android.animation.sometransactionforfragmet" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SlideFrag"
            android:label="@string/title_activity_SlideFrag" >
        </activity>
        <activity
            android:name=".SlideFragSupport"
            android:label="@string/title_activity_SlideFragSupport" >
        </activity>
    </application>

</manifest>
